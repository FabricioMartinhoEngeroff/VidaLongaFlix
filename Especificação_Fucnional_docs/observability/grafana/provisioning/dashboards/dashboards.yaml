apiVersion: 1

providers:
  - name: VidaLongaFlix
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10     # recarrega se o JSON mudar — útil em dev
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: false
